# 太炅 Lotto Lab v30 사용방법

GitHub Actions에서 생성된 `Taegyeong_Lotto_Lab_v30_Windows.zip`을 다운로드해 압축을 풉니다.

실행:
- `Taegyeong_Lotto_Lab_v30.exe`

같은 폴더에 반드시 있어야 하는 파일:
- `research_worker.exe`

검은 CMD 창은 표시되지 않습니다.

실행 오류가 발생하면 같은 폴더의 `error.log`와 `startup.log`를 확인합니다.
